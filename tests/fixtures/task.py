# import pytest
# from tmaps.experiment.setup import TaskSubmission


# @pytest.fixture(scope='function')
# def tasksub(testexp, testuser):
#     # TODO: Need to first create an actual task and insert
#     # it into the gc3pie table.
#     t = TaskSubmission.create(
#         submitting_user_id=testuser.id,
#         experiment_id=testexp.id,
#         task_id=1)
#     return t


