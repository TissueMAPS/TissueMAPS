---
version: 0.1.0

input:

    - name: objects
      type: SegmentedObjects
      key:
      help: Objects that should be rasterized on a label image.

    - name: plot
      type: Plot
      value: false
      help: Should a figure be created?

output:

    - name: label_image
      type: LabelImage
      key: rasterize.label_image
      help: >
        Labeled image with pixels of each connected component
        having a unique non-zero value.

    - name: figure
      type: Figure


