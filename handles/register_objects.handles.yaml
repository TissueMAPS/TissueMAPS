---
version: 0.0.1

input:

    - name: label_image
      type: LabelImage
      key:
      help: >
        Labeled image containing the objects whose segmentation should be saved.

output:

    - name: objects
      type: SegmentedObjects
      key:
      help: Name that should be given to the objects in "label_image".
