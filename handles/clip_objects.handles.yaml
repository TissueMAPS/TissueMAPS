---
version: 0.0.1

input:

    - name: image_1
      type: LabelImage
      key:
      help: Labeled image that should be clipped.

    - name: image_2
      type: LabelImage
      key:
      help: Labeled image that's used as clipping mask.

    - name: plot
      type: Plot
      value: false
      help: Should a figure be created?

output:

    - name: clipped_image
      type: LabelImage
      key: clip_objects.clipped_image
      help: Clipped mask.

    - name: figure
      type: Figure
      help: Comparison of "input_image_1" and "clipped_image"



