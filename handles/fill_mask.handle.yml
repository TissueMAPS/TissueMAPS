---
input:

    - name: mask
      mode: pipe
      kind: image
      value: 
      help: >
        Binary image in which holes of connected components should be filled.

output:

    - name: filled_mask
      mode: pipe
      kind: image
      id: fill_mask.filled_mask
      help: Filled binary image.


