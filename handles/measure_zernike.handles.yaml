---
input:

    - name: label_image
      type: SegmentedObjects
      key: 
      help: >
        Labeled image containing the objects
        for which Zernike features should be extracted.

    - name: plot
      type: Plot
      value:
      options: [true, false]
      help: Should a figure be created?

output:

    - name: measurements
      type: Measurement
      objects_ref: label_image
      help: Extracted Zernike features.

    - name: figure
      type: Figure


