---
version: 0.0.1

input:

    - name: input_mask_1
      type: BinaryImage
      key:
      help: First binary mask.

    - name: input_mask_2
      type: BinaryImage
      key:
      help: Second binary mask.

    - name: plot
      type: Plot
      value: false
      help: Should a figure be created?

output:

    - name: output_mask
      type: BinaryMask
      key: combine_objects.output_mask
      help: Combined mask.

    - name: figure
      type: Figure




